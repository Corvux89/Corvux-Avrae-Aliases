<drac2>
c = character()
out = []
if c.get_cvar('subclass'):
    classes = load_json(c.get_cvar('subclass'))
    for c in load_json(c.get_cvar('subclass')):
        out.append(classes.get(c))

if bool(set(out) & {"Synthesis", "Technology", "Aqinos"}):
    return err("Here")
else:
    return err("Nope")
</drac2>